package com.looply.social.icons

/**
 * Fallback para ícones em falta no pack SVG.
 * Adiciona entradas aqui à medida que adicionares drawables locais
 * equivalentes em res/drawable/.
 */
object MaterialFallback {
    fun forName(name: String): Int? = null
}